name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: action/checkout@v2
      - name: Print text from index.js
        run: cat dist/index.js
